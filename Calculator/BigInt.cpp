#include <iostream>
#include <vector>
#include <fstream>

class BigInteger {
    public: 

    BigInteger(std::string &value){
        
    }

    void Input(){
        char c;
        long long buff = 0, check = 0, tmp = 0;
        while (c = getchar() != '\n'){
            if (check == 5){
                buff 
            }
        }   
    }

    private:
    
    long long base_number_system = 1000000;
    std::vector<long long> values;
};

int main(){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);
    
}